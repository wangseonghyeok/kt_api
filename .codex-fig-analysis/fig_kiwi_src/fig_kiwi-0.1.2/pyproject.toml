[project]
name = "fig_kiwi"
requires-python = ">= 3.7"
readme = "README.md"
dynamic = ["version"]

[project.urls]
"Source Code" = "https://github.com/sketch-hq/fig_kiwi"

[build-system]
requires = ["maturin>=0.14,<0.15"]
build-backend = "maturin"

[tool.black]
line-length = 99
